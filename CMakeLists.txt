CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

PROJECT(flame)

FILE(GLOB_RECURSE HEADER_LIST src/flame/*.h*)
FILE(GLOB_RECURSE SOURCE_LIST src/flame/*.c*)

SOURCE_GROUP("Head" FILES ${HEADER_LIST})
SOURCE_GROUP("Source" FILES ${SOURCE_LIST})

ADD_LIBRARY(flame STATIC ${HEADER_LIST} ${SOURCE_LIST})

ADD_SUBDIRECTORY(tools)