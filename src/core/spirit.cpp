#include "spirit.h"

namespace tke
{
	void Spirit::show()
	{
		//glBindTexture(GL_TEXTURE_2D, tex);
		//GLfloat x = TKE2_C2VX(coord.x), y = TKE2_C2VY(coord.y), w = TKE2_C2CX(size.x), h = TKE2_C2CY(size.y);
		//GLfloat vertex[] = {
		//	x, y,
		//	x + w, y,
		//	x, y + h,
		//	x + w, y + h
		//};
		//glBindBuffer(GL_ARRAY_BUFFER, tke2_vbo_vertex_quad);
		//glBufferData(GL_ARRAY_BUFFER, 4 * sizeof(glm::vec2), vertex, GL_STATIC_DRAW);
		//tke2_vao_quad.draw();
	}
}
